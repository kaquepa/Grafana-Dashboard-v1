# Criar arquivo: ./grafana/provisioning/datasources/postgres.yml

apiVersion: 1

datasources:
  - name: postgres
    type: postgres
    access: proxy
    url: postgres:5432
    database: Grafana_DB
    user: Grafana_Admin
    jsonData:
      sslmode: disable
      postgresVersion: 1500  # PostgreSQL 15
      timescaledb: false
    secureJsonData:
      password: secure_password_123
    isDefault: true
    editable: true